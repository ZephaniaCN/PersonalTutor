"""Adaptive quiz capability package.

* :mod:`quiz` — framework-agnostic coordinator (next_question / grade_one),
  reused by the REST layer and the capability wrapper.
* :mod:`capability` — ``personal_quiz`` capability for ``deeptutor run``.
"""
